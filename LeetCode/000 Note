xor
We can prove following properties by truth table
  - A ^ B = B ^ A
  -(A ^ B) ^ C = A ^ (B ^ C)
  - A ^ A = 0
  - A ^ A ^ B = B

1.
Exchange 2 variable without extract memory
B = A ^ B
A = B ^ A // A ^ B ^ A = B
B = B ^ A // A ^ B ^ B = A

2. LeetCode 136
A ^ B ^ C ^ D ^ A ^ D ^ B =  A ^ A ^ B ^ B ^ D ^ D ^ C = C

==========
